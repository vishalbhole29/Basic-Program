# Remove empty tuples from a list

tuples = [(), ('ram', '15', '8'), (), ('laxman', 'sita'),
          ('krishna', 'akbar', '45'), ('', ''), ()]
def Remove(tuples):
  tuples = [t for t in tuples if t]
  return tuples

print(Remove(tuples))
